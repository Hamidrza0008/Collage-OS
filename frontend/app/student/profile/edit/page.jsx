import EditProfileAssembler from "@/components/profile/edit/EditProfileAssembler";

export const metadata = {
  title: "Edit Profile & Portfolio — College OS",
  description: "Manage your student profile, technical skills, showcased projects, and campus visibility.",
};

export default function EditProfilePage() {
  return <EditProfileAssembler />;
}
