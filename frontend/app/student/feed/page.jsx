import CampusFeed from "@/components/campus-feed/CampusFeed";

export const metadata = {
  title: "Campus Feed - College OS",
  description:
    "Share moments, interact with your peers, and stay connected with everything happening on campus.",
};

export default function CampusFeedPage() {
  return <CampusFeed />;
}
