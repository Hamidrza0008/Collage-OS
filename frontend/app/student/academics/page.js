import Academics from "@/components/academics/Academics";

export const metadata = {
  title: "Academics - College OS",
  description: "Track your academic progress, view grades, and stay on top of your learning journey.",
};

export default function AcademicsPage() {
  return <Academics />;
}
