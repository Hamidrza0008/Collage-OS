import AcademicTimetableAssembler from "@/components/academics/timetable/AcademicTimetableAssembler";

export const metadata = {
  title: "Academic Timetable & Master Calendar | College OS",
  description:
    "Plan classes, assessments, deadlines, exams, and campus events from one unified calendar workspace.",
};

export default function AcademicTimetablePage() {
  return <AcademicTimetableAssembler />;
}
