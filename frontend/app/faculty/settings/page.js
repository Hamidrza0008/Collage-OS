import Settings from "@/components/settings/Settings";

export const metadata = {
  title: "Settings - Faculty Portal - College OS",
  description: "Manage your account, preferences, and College OS experience.",
};

export default function FacultySettingsPage() {
  return <Settings />;
}
